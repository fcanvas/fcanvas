export type ClientRectOptions = {
  // eslint-disable-next-line functional/prefer-readonly-type
  skipTransform?: boolean;
  // eslint-disable-next-line functional/prefer-readonly-type
  skipStroke?: boolean;
  // eslint-disable-next-line functional/prefer-readonly-type
  skipShadow?: boolean;
};
